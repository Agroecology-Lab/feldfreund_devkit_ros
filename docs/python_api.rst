=====================
Python API Reference
=====================

Auto-generated reference for the Python packages under ``src/``.
Each entry below recurses into every submodule, class and function and
generates its own page (see ``autosummary_generate`` / ``:recursive:``
in ``conf.py``).

.. note::
   ``devkit_bringup`` and ``devkit_simulation`` contain no importable
   Python modules (launch files, URDF, worlds and config only), so they
   have no entry here. ``ros2graph_explorer`` and ``ublox_dgnss`` are
   currently empty in this checkout.

.. autosummary::
   :toctree: _autosummary
   :recursive:

   devkit_driver
   devkit_f2c_planner
   devkit_mavlink_bridge
   devkit_ui
